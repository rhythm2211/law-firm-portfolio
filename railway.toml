[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"

# Recommended: create a Railway volume with mount path /data (Ctrl+K → "Add Volume")
# requiredMountPath = "/data"
